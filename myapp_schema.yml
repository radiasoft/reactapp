models:
    dog:
      breed: PKString("")
      gender: Gender("male")
      height: PKRangedFloat(0.5, min_val=0, units="m")
      weight: PKRangedFloat(60.5, min_val=0, units="lb")
      disposition: DogDisposition("friendly")
      favoriteTreat: PKString("")

types:
  Gender: PKChoices(("male", "female",))
  DogDisposition: PKChoices(("aggressive", "friendly", "submissive",))
